public class ternary {
    public static void main(String[] args) {
        int n = 4;
        int result = 0;
//        if (n%2==0)
//            result = 10;
//        else
//            result = 20;
        result = n%2==0 ? 10 : 20;
        System.out.println(result);
        //if else if nested ternary example
//        int number = -1 or 1 or 0;
//        String result = (number > 0) ? "Positive" : (number < 0) ? "Negative" : "Zero";
//        System.out.println(result);
    }
}
